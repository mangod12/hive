---
description: Step-by-step interactive builder for goal-driven agents. Qualifies use cases, creates package structure, defines goals, designs nodes and graph, then generates the Python package. Use when actively building a new agent from scratch or from a template.
when_to_use: |
  - Building a new agent from scratch
  - Customizing an existing template agent
  - Have clear requirements and ready to write code
  Produces: exports/agent_name/ Python package with goal, nodes, edges, CLI
---

use hive-create skill
